syntax = "proto3";//版本
option java_outer_classname = "StudentPOJO";//生成的外部类名，同时也是.java文件的文件名
//protobuf使用message管理数据
message Student{//会在StudentPOJO 外部类生成一个内部类Student 是真正要发送的POJO对象
    int32 id =1;//Student类中有一个属性， 名字为id 类型为int, 1表示属性序号，不是值
    string name = 2;
}